<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Adding Data</title>

<style>

table {
  width: 100%;
  border-collapse: collapse;
  background: #fff;
}
.btn-create {
  background: #0b0784;
  color: #fff;
  width: 100%;
  height: 40px;
  margin-bottom: 20px;
}

th, td {
  padding: 12px;
  border-bottom: 1px solid #ddd;
  text-align: center;
}

th {
  background: #e8f3ff;
}

/* FORM */
.form-group {
  margin-bottom: 12px;
}

input, select {
  width: 100%;
  padding: 10px;
  border: 1px solid #ccc;
  border-radius: 4px;
}

</style>

</head>
<div class="container">
  <h2>JSON CRUD (Create, Read, Update, Delete)</h2>
<a href="/add">

    <button class="btn-create">Create</button>
  </a>

  <table>
    <thead>
      <tr>
        <th>No</th>
        <th>Name</th>
        <th>Height</th>
        <th>Weight</th>
        <th>Birth Date</th>
        <th>Is Married</th>
        <th>Actions</th>
      </tr>
    </thead>
    <tbody>
   <tbody>
 
  
 
   <tbody>
    <% if (karyawan && karyawan.length > 0) { %>
      <% karyawan.forEach((k, index) => { %>
        <tr>
          <td><%= index + 1 %></td>
          <td><%= k.name %></td>
          <td><%= k.height %></td>
          <td><%= k.weight %></td>
          <td><%= 
    String(new Date(k.birthdate).getDate()).padStart(2, '0') + '-' +
    String(new Date(k.birthdate).getMonth() + 1).padStart(2, '0') + '-' +
    String(new Date(k.birthdate).getFullYear()).slice(-2)
  %></td>
          <td><%= k.married %></td>
          <td>
            <a href="/karyawan/edit/<%= k.id %>">Edit</a> |
            <a href="/karyawan/delete/<%= k.id %>"
               onclick="return confirm('Yakin hapus?')">Delete</a>
          </td>
        </tr>
      <% }) %>
    <% } else { %>
      <tr>
        <td colspan="7">Data kosong</td>
      </tr>
    <% } %>
  </tbody>

</tbody>


  </table>
</div>

</body>
</html> 