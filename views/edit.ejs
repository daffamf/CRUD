<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Updating Data</title>
    <style>
  
        * {
            box-sizing: border-box;
            font-family: Arial, Helvetica, sans-serif;
        }

        body {
            background: #eaf6ff;
            margin: 0;
            padding: 0;
        }

        .container {
            width: 90%;
            max-width: 900px;
            margin: 40px auto;
            background: #f5fbff;
            padding: 20px;
            border-radius: 6px;
            box-shadow: 0px 4px 10px rgba(0,0,0,0.1);
        }

        h2 {
            text-align: center;
            margin-bottom: 20px;
        }

        .form-group {
            margin-bottom: 12px;
        }

        input, select {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
        }

        .btn {
            width: 100%;
            padding: 12px;
            border: none;
            cursor: pointer;
            font-size: 14px;
            border-radius: 4px;
            margin-top: 10px;
            text-align: center;
            display: block;
            text-decoration: none;
        }

        .btn-save {
            background: #20c997;
            color: white;
        }

        .btn-cancel {
            background: #4b32ff;
            color: white;
        }
    </style>
</head>
<body>

<div class="container">
    <h2>Update data karyawan</h2>

    <form action="/edit/<%= item.id %>" method="POST">
        
        <div class="form-group">
           <input type="text" name="name" value="<%= item.name %>" required><br><br>
        </div>

        <div class="form-group">
          <input type="number" name="height" value="<%= item.height %>" required><br><br>
        </div>

        <div class="form-group">
         <input type="number" step="0.01" name="weight" value="<%= item.weight %>" required><br><br>
        </div>

        <div class="form-group">
           <input type="date" name="birthdate" value="<%= item.birthdate.toISOString().split('T')[0] %>" required><br><br>
        </div>

        <div class="form-group">
            <select name="married">
        <option value="true" <%= item.married ? 'selected' : '' %>>True</option>
        <option value="false" <%= !item.married ? 'selected' : '' %>>False</option>
    </select><br><br>
        </div>

        <button type="submit" class="btn btn-save">Save</button>
    </form>

    <a href="/" class="btn btn-cancel">Cancel</a>
</div>

</body>
</html>